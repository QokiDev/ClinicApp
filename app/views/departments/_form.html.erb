<div class="card-body">
<%= form_with(model: department, class: 'needs-validation', novalidate: true) do |form| %>
  <% if department.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
        <% department.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= form.label :name, class: 'form-label' %>
      <%= form.text_field :name, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= form.label :address, style: 'form-label' %>
      <%=  form.text_field :address, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= form.label :founding_date, class: 'form-label' %>
      <%= form.date_field :founding_date, class: 'form-control' %>
    </div>

    <div class="text-center">
      <%= form.submit 'Save', class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>
