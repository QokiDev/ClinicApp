<div class="card-body">
<%= form_with(model: patient_card, class: 'needs-validation', novalidate: true) do |form| %>
  <% if patient_card.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(patient_card.errors.count, "error") %> prohibited this patient card from being saved:</h2>

      <ul>
        <% patient_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
      

  <div class="form-group">
    <%= form.label :department_id, style: 'form-label' %>
    <%=  form.text_field :department_id, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :doctor_id, class: 'form-label' %>
    <%= form.text_field :doctor_id, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :patient_id, class: 'form-label' %>
    <%= form.text_field :patient_id, class: 'form-control' %>
  </div>

  <div class="text-center">
    <%= form.submit 'Save', class: 'btn btn-primary' %>
  </div>

  <% end %>
</div>