<div class="card-body">
<%= form_with(model: doctor, class: 'needs-validation', novalidate: true) do |form| %>
  <% if doctor.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(doctor.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
        <% doctor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= form.label :name, class: 'form-label' %>
      <%= form.text_field :name, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= form.label :department_id, style: 'form-label' %>
      <%=  form.text_field :department_id, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= form.label :specialization_id, class: 'form-label' %>
      <%= form.text_field :specialization_id, class: 'form-control' %>
    </div>

    <div class="text-center">
      <%= form.submit 'Save', class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>